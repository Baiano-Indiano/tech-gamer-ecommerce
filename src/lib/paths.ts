/**
 * Caminhos de importação da aplicação
 * 
 * Exemplo de uso:
 * import { Button } from '@/components/ui/Button';
 * import { useAuth } from '@/features/auth/hooks/useAuth';
 */

export {}; // Apenas para evitar erro de módulo vazio
